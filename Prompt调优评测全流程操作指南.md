# Prompt 自动化调优与评测 - 全流程操作指南

## 📌 重要说明（使用前必读）

1. **本系统定位**：Prompt开发测试工具，非生产部署环境
2. **工作本质**：开发(DEV)过程，需在类似Claude Code的Agent编程环境中运行和调试
3. **使用预期**：随时准备修改数据与代码，而非运行固化应用
4. **缓冲期**：建议先验证流程和机制的有效性

---

## 一、项目准备

### 1.1 环境配置
```bash
# 创建虚拟环境
conda create -n vc python=3.8
conda activate vc

# 安装依赖
cd Virtual-Coach-main
pip install -e .
```

### 1.2 配置API密钥
在 `code/models/` 目录下创建 `api_keys.py`，根据实际使用的模型填写：

**GLM(智谱)示例：**
```python
GLM_URL="your_api_url"
GLM_API_KEY="your_api_key"
GLM_MODEL="glm-4-flash"
```

**其他模型（如OpenAI、讯飞等）请相应修改字段名**

### 1.3 生成LLM调用文件
```bash
cd code/utils 
python auto_generate_llm_call.py
```

### 1.4 并发数设置
> 根据所用Target模型的实际能力设置。部分模型（如glm-4-flash）不支持并发，需设置为1。

---

## 二、标准工作流程（8步）

```
需求分析 → Prompt设计 → 评测数据 → 评测脚本 → 运行评测 → 结果分析 → 优化建议 → 版本迭代
```

### 第1步：明确业务需求
- 输入场景名称（如：用户基本信息提取）
- 输入任务需求描述（用户需要提供什么信息）
- **需求描述完成后，需单独输入 `-and` 以结束输入**

### 第2步：设计Prompt
- Helper模型自动生成初始prompt
- 用户可选择：确认(C) / 编辑(E) / 重新生成(R)
- 确认后保存为 V1 版本

### 第3步：生成评测数据
两种模式：
- **全自动生成**：输入数量，直接生成测试样本（可能不理想）
- **种子扩充**（推荐）：提供示例，LLM扩展生成更多样本

> 业务场景中，推荐使用"种子扩充"模式，以生成更贴合需求的数据。

### 第4步：生成评测脚本
- 系统自动生成可独立运行的评测脚本
- 评测逻辑会区分两类字段：
  - 分类、枚举、数字、布尔、数组长度、字段层级等“硬结构字段”严格匹配
  - `prompt_start`、feedback、summary、message 等自然语言字段允许措辞差异，并在需要时自动调用 Helper Judge 做语义复判

> **注意**：如果业务要求某段话术必须逐字固定，请在需求和 prompt 中明确写出“必须逐字输出固定模板”；否则 ground truth 会被视为参考答案，而不是快照答案。

### 第5步：运行评测
- 并行评测（并发数根据模型能力设置）
- 输出：准确率 + 耗时统计

### 第6步：汇报评测结果
- 显示各数据项的评测结果
- 标记正确/错误样本

### 第7步：获取优化建议
- 分析错误样本
- Helper模型给出改进建议

### 第8步：迭代优化
- 修改prompt → 保存新版本 → 重新评测
- 版本命名：`data/prompt/{场景名}/v1.py`, `v2.py`, ...

---

## 三、效率测试（每个Prompt需单独测试）

1. 先测试**准确率**（标准评测）
2. 再测试**效率**（耗时分析）
3. 记录两种结果到 `data/eval_result/`

---

## 四、项目文件结构规范

| 用途 | 路径 |
|------|------|
| Prompt及版本 | `data/prompt/{场景名}/v*.py` |
| 评测脚本 | `code/eval/eval_prompt_{场景名}.py` |
| 评测数据集 | `data/eval/{场景名}_data.json` |
| 评测结果 | `data/eval_result/{场景名}_{时间戳}.json` |

---

## 五、启动命令

```bash
python main.py
```

---

## 六、关键说明

1. **Helper模型**：用于生成prompt和数据、提供优化建议
2. **Target模型**：被评测的实际模型
3. **版本管理**：每次修改自动保存新版本，支持断点续接
4. **数据命名**：评测结果按时间戳自动命名
5. **GitHub同步**：代码和数据变更需同步到Virtual-Coach仓库
