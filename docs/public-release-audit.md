# 公开版审查记录（2026-09-03）

检查基线：`16bae14128562efa55f6421e2b3eb2d4ef79bde5`。检查时 GitHub 的 `main` 与本地一致，远程列出的分支只有 `main`，无标签；公开 Releases 与 Actions artifacts 为空。

## 已确认的问题与修复

| 问题 | 影响 | 本次处理 |
| --- | --- | --- |
| 提交作者及提交者使用个人邮箱 | 历史元数据可被公开读取 | 后续提交使用 GitHub noreply；旧历史需要另行批准重写 |
| 四个分析脚本含个人电脑路径 | 暴露目录结构，且在其他机器无法运行 | 改为相对源码位置定位示例 |
| 旧模型生成器把密钥写入生成源码 | 后续提交生成文件可能泄露真实凭据 | 统一使用运行时配置；密钥、地址、模型名均不进入生成源码 |
| GPT 默认网关与 OpenAI 密钥混用、Kimi 自动读取 OpenAI 密钥 | 可能将凭据和输入发送给非预期服务 | GPT 默认官方端点；Kimi 使用自己的配置前缀 |
| GUI 自动复用供应商密钥作为网关密钥并保存 | 错误服务接收密钥，磁盘留有额外凭据副本 | 网关专用凭据、不再保存密钥，读取旧配置时清除旧字段 |
| 工作流服务允许任意跨域来源与本地文件输入 | 网页可能启动模型任务或诱导读取本地配置 | 本机与同源限制、Host 校验、示例目录限制、同源 UI |
| 新场景和工作流 JSON 日志缺少默认忽略 | 输入、输出和业务资产可能随 `git add` 进入公开仓库 | 扩大默认忽略范围，加入完整暂存区扫描和 CI |
| 跨语言测试内嵌业务场景规则 | 与“完全不含业务 Prompt”的公开声明不一致；来源不明确 | 换成新写的虚构图书馆演示和配套数据 |
| 示例包含非演示域名邮箱、可拨号格式号码与漂移年龄 | 容易被误认为真实数据，且测试结果随时间失效 | `example.com`、占位号码、明确年龄 |
| 安装路径、Python 版本、历史示例说明不准确 | 按 README 操作失败，误以为所有脚本开箱即用 | 修正文档、批处理工作目录、语法错误和运行限制说明 |

## 检查结论与边界

在检查的公开文件及可达 Git 历史中，未发现确认属于真实服务凭据的硬编码密钥。历史中的格式模板、节点类型标记和字段引用已按上下文区分。这是本次检查结果，不是对未知密钥格式、编码内容或数据授权的绝对保证。

未执行真实模型调用、网关部署、密钥有效性验证或对外测试。运行测试仅使用虚构数据；服务验证使用本地测试客户端。GUI 凭据测试覆盖配置方法，未运行完整桌面交互测试。

## 仍需维护者处理

1. **历史隐私**：普通提交无法清除旧邮箱、旧路径及被替换的业务示例。若要彻底清理当前仓库的可达历史，需要明确批准重写公开历史；已有克隆与缓存仍可能保留旧内容。
2. **上游来源**：上游 `pyproject.toml` 声明 MIT，但未获取独立许可证文本与完整版权声明；细节见 [来源说明](../THIRD_PARTY_NOTICES.md)。
3. **非代码附件与未取得的状态**：没有将本地忽略文件、完整仓库权限设置或第三方克隆视为已审计对象。后续新增附件与业务示例应单独复核。

## 发布验证

修复提交 `97fa7ce` 的 49 项离线回归测试通过，Python/JSON 解析检查、完整暂存区扫描通过；[GitHub Actions 检查](https://github.com/xiaohuanglian/prompt-debug-eval-system/actions/runs/33740932237)通过。

发布后读取 GitHub 密钥告警接口时发现仓库原先未启用密钥扫描。现已启用仓库级密钥扫描和推送保护；没有启用向供应商验证凭据有效性的选项。开启状态不等于 GitHub 的历史扫描已经结束，也不能替代本记录中的人工判断。

复核命令：`python scripts/check_public_release.py`、`python scripts/check_public_release.py --staged`、`python scripts/check_public_release.py --history`、`python -m unittest discover -s tests -v`（测试依赖见 `tests/requirements.txt`）。历史命令预期继续报告尚未重写的旧记录。
