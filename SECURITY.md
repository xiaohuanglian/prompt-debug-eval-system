# 公开发布与本地安全

本项目用于本地 Prompt 开发与评测。调用模型会将输入文档、Prompt 和评测内容发送到所配置的模型服务；先确认自己有权向该服务提供这些内容。

## 凭据和生成文件

- 凭据仅保存在本地受信任配置或环境变量中；`api_keys.py`、`llm_key.py`、`.env*`、`.deploy_config/` 不应提交。
- 模型生成器只生成通用适配器，具体密钥、服务地址和模型名在运行时读取。生成操作本身不调用远程模型。
- 网关与模型供应商使用各自的凭据。GUI 不再持久化网关密钥，读取旧配置时会移除其中的密钥字段；磁盘备份中的旧副本不会因此被删除。
- 新场景、评测数据、自动生成的评测脚本/模型文件、工作流日志和结果默认被忽略。已经跟踪的文件仍会出现在提交中，修改公开示例时仍需审阅差异。
- 日志可能含完整输入和模型回复。分享截图、错误日志、压缩包或评测结果前需要另外脱敏；`.gitignore` 只约束 Git 的默认添加行为。

## HTTP 服务边界

工作流服务没有多用户认证，只允许本机客户端与同源浏览器页面。按工作流 README 以 `127.0.0.1` 启动，并禁用代理头信任。不要将其放到公网、共享端口或反向代理后。`example_path` 仅允许 `example/` 中的 JSON 文件。

本地 CLI、模型适配器和 Python Prompt 文件属于受信任代码，可能被执行；本项目不提供运行陌生 Python 文件的沙箱。

## 发布检查与历史限制

`scripts/check_public_release.py` 使用离线规则检查常见凭据格式、个人路径、非演示邮箱、私网地址与敏感文件。只输出位置和规则，不打印匹配值。规则扫描不能判断数据授权、知识产权归属或所有形式的密钥，需要人工复核。

`--staged` 用于发布前检查；`--history` 用于事件排查，会读取可达历史与作者/提交者邮箱。普通修复提交、删除文件或增加忽略规则，都不能擦除已公开的旧提交。

若确认有真实密钥公开，应先在对应供应商撤销或轮换，再协调清理 Git 历史、旧引用和发布附件。历史重写会改变提交标识，需要明确确认；已有克隆、缓存和第三方副本也可能仍保留旧内容。后续提交建议使用 GitHub 提供的 noreply 邮箱。

请勿把真实凭据或个人数据贴入公开 Issue。可使用 GitHub 私密漏洞报告功能（仓库启用时），或先通过维护者公开提供的私下联系方式沟通。
